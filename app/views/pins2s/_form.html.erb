<%= form_for @pins2, html: { multipart: true } do |f| %>
  <% if @pins2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pins2.errors.count, "error") %> prohibited this pins2 from being saved:</h2>

      <ul>
      <% @pins2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
